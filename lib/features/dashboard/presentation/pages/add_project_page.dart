import 'package:flutter/material.dart';

class AddPorjectpage extends StatelessWidget {
  const AddPorjectpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Add Project Page'),
    );
  }
}
